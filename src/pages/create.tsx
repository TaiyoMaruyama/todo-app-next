import { Layout } from "@/components/Layout";
import { Create } from "@/components/Create";

const CreatePage = () => {
  return (
    <>
      <Layout>
        <Create />
      </Layout>
    </>
  );
};

export default CreatePage;
